{
  "name": "mismcp",
  "private": true,
  "license": "MIT",
  "type": "module",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.5.2"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.18.25",
    "@opencode-ai/sdk": "^1.18.25",
    "@types/node": "^22.5.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node --experimental-sqlite dist/mcp-server.js",
    "test": "npm run build && node --test \"dist/*.test.js\"",
    "test:e2e": "npm run build && tsc -p tsconfig.e2e.json && node --experimental-sqlite dist/e2e-bus-check.js"
  }
}
